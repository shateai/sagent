{
  "entities": {
    "agent_status": {
      "title": "Agent Status",
      "description": "Simulation state and metrics for the wealth-building AI agent",
      "type": "object",
      "properties": {
        "status": {
          "type": "string",
          "enum": ["sleeping", "planning", "acting"],
          "description": "The current state of the agent"
        },
        "capital": {
          "type": "number",
          "description": "Simulated balance or capital in USD"
        },
        "hourlyRate": {
          "type": "number",
          "description": "The rate at which the agent earns or simulates earnings per hour"
        },
        "lastWoken": {
          "type": "string",
          "description": "ISO timestamp of the last time the agent woke up to plan"
        },
        "mainStrategy": {
          "type": "string",
          "description": "The primary strategy or focus area of the agent currently"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of the status update"
        }
      },
      "required": ["status", "capital", "hourlyRate", "lastWoken", "mainStrategy", "updatedAt"]
    },
    "wealth_plans": {
      "title": "Wealth Plan",
      "description": "An hourly plan generated by the AI on how to acquire wealth",
      "type": "object",
      "properties": {
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of the plan creation"
        },
        "hourlyHour": {
          "type": "number",
          "description": "The simulation cycle counter or sequence number of the hour"
        },
        "topic": {
          "type": "string",
          "description": "Scope/market sector of this hour's speculation"
        },
        "thoughtProcess": {
          "type": "string",
          "description": "Markdown string containing the detailed research, thought process and planning"
        },
        "actionSteps": {
          "type": "array",
          "description": "High-level action steps for execution"
        },
        "estimatedFeasibility": {
          "type": "number",
          "description": "Feasibility score from 0 to 100"
        },
        "simulatedReturn": {
          "type": "number",
          "description": "Expected simulated earnings in USD from this plan"
        }
      },
      "required": ["createdAt", "hourlyHour", "topic", "thoughtProcess", "estimatedFeasibility", "simulatedReturn"]
    },
    "user_messages": {
      "title": "User Message",
      "description": "Interaction messages with the live Gemini Assistant",
      "type": "object",
      "properties": {
        "role": {
          "type": "string",
          "enum": ["user", "assistant"],
          "description": "Author of the message"
        },
        "content": {
          "type": "string",
          "description": "The message text"
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of the message"
        }
      },
      "required": ["role", "content", "createdAt"]
    }
  },
  "firestore": {
    "agent_status/main": {
      "schema": "agent_status",
      "description": "The singleton state documentation for the active planning agent"
    },
    "wealth_plans/{planId}": {
      "schema": "wealth_plans",
      "description": "Individual strategic logs written of each hourly planning cycle"
    },
    "user_messages/{messageId}": {
      "schema": "user_messages",
      "description": "Dynamic workspace chat log between user and the Gemini AI consult handler"
    }
  }
}
